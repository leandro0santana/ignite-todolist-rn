import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    borderTopWidth: 1,
    borderColor: '#333333',
    height: 210,
    padding: 20,
    justifyContent: 'center',
    alignItems: 'center',
    gap: 12,
    paddingTop: 52,
  },

  emptyTextBold: {
    fontWeight: 'bold',
    color: '#808080',
    fontSize: 14,
    textAlign: 'center',
  },

  emptyText: {
    color: '#808080',
    fontSize: 14,
    textAlign: 'center',
  },
})
